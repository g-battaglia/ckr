# Stuff for mainland KR

- Use more "native" swisseph functions
  - `get_planet_name()`
- Create new "house like" points for:
  - Ascendant
  - Midheaven
  - Descendant
  - Imum Coeli
  - Vertex
- Create new points for the missing 0/24 standard points
- Optimize the code for a general function that calculates the information and returns the AstrologicalSubject model.